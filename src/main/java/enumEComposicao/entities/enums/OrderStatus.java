package enumEComposicao.entities.enums;

public enum OrderStatus {
    //PENDING_PAYMENT(aguaradando pagamento)-PROCESSING(PROCESSANDO)-  SHIPPED(ENVIADO) - DELIVERED(ENTREGUE)

    PENDING_PAYMENT,
    PROCESSING,
    SHIPPED,
    DELIVERED;
}
